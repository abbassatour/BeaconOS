
للوصول بتجربة **Beacon OS** إلى مستوى أنظمة التشغيل الرائدة عالمياً (مثل معايير **Apple HIG - Spatial Computing** و **Google Material 3 Motion Physics**)، يجب ألا نتعامل مع الأنيميشن كمجرد "مؤثرات بصرية" أو دوال `Duration` و `Curve` تقليدية، بل كـ **نظام فيزيائي متكامل (Physics-based Motion System)** يحاكي حركة الأجسام في الفضاء الحقيقي ويحترم ميزانية الإطار الصارمة (**Frame Budget: 16.6ms لـ 60Hz و 8.33ms لـ 120Hz**).

فيما يلي **الخطة الهندسية الشاملة والمفصلة للمرحلة الثانية: هندسة الحركة والتنقل الفضائي (Spatial Motion & Gesture Architecture)**، مصممة وفق أفضل ممارسات فرق هندسة النظم في الشركات الكبرى.

---

# 📐 الميثاق الحركي للنظام (BeaconOS Motion System Architecture)

```
                       ┌───────────────────────────────┐
                       │     Unified Gesture Arena     │
                       │  (Disambiguation & Routing)   │
                       └──────────────┬────────────────┘
                                      │ Velocity & Delta
                                      ▼
                       ┌───────────────────────────────┐
                       │  RK4 Spring Physics Engine   │
                       │  (Stiffness, Damping, Mass)   │
                       └──────────────┬────────────────┘
                                      │ Value Streams (XY & Z)
                 ┌────────────────────┴────────────────────┐
                 ▼                                         ▼
   ┌───────────────────────────┐             ┌───────────────────────────┐
   │ Spatial Culling & Render  │             │ Haptic-Motion Detents     │
   │ (Repaint Boundaries/Lazy) │             │ (Tactile Sensory Sync)    │
   └───────────────────────────┘             └───────────────────────────┘
```

---

## 🏛️ المحور الأول: استبدال المنحنيات الزمنية بالنوابض الفيزيائية (Spring Physics Engine)

### 1. المشكلة في الكود الحالي:

الكود الحالي يعتمد على دوال وقتية ثابتة مثل:

```dart
AnimatedSlide(offset: ..., duration: Duration(milliseconds: 320), curve: Curves.fastOutSlowIn)
```

**لماذا يُعد هذا غير مقبول في معايير الأنظمة الكبرى؟**

* إذا ألقى المستخدم الشاشة بسرعة عالية (High Velocity Fling)، تتجاهل دوال `Duration` قوة الرمية تماماً وتتحرك بالسرعة الثابتة نفسها، مما يسبب شعوراً بـ "الانفصال الفيزيائي" (Viscous Disconnect).
* إذا غيّر المستخدم اتجاه حركته في منتصف الطريق، يحدث انكسار فجائي (Animation Glitch) لإيقاف الأنيميشن القديم وبدء الجديد.

### 2. الحل الهندسي المعتمد:

بناء محرك نوابض فيزيائي (**SpringSimulation**) يرتكز على معادلات فيزيائية ديناميكية ثلاثية المعاملات:

1. **الصلابة (Stiffness / Tension):** تحدد قوة استجابة الواجهة وجذبها للهدف (مثال: `300.0 - 400.0`).
2. **التخميد (Damping Ratio):** ضبط النوابض لتكون **Critically Damped** ($\zeta \approx 1.0$) أو شبه مفرطة التخميد لمنع الارتداد المزعج (Bouncing) غير اللائق بنظام تشغيل جاد، وضمان التوقف بنعومة حريرية.
3. **الكتلة الافتراضية (Mass):** ضبط وزن الواجهات الفضائية لتعطي إحساساً بالعمق والفخامة (مثال: `1.0`).
4. **تمرير السرعة المتجهة (Velocity Injection):** أخذ السرعة الحقيقية لإصبع المستخدم في لحظة الإفلات (`details.velocity.pixelsPerSecond`) وتغذية المحرك بها فوراً ليواصل المشهد حركته بالزخم نفسه.

---

## ✋ المحور الثاني: هندسة حلبة الإيماءات وفض النزاع (Gesture Disambiguation Architecture)

### 1. المشكلة في الكود الحالي:

في `spatial_compass_page.dart`، هناك خليط عشوائي بين:

* `Listener` أصلي يستمع لكل Pointers.
* `GestureDetector` يستمع لـ `Scale` (لأنه يرصد إصبعين للـ Pinch).
* `CustomScrollView` داخل كل غرفة يريد بدوره الاستماع للتمرير الرأسي!

**النتيجة:** صراع على استهلاك اللمسات (Touch Contention)، حيث يتأرجح النظام بين التمرير الرأسي للصفحة وبين اعتبار الحركة انتقالاً للغرفة المجاورة أو قفزاً للطابق الثاني.

### 2. الحل الهندسي المعتمد:

بناء **موزع إيماءات مخصص (Custom Gesture Coordinator)**:

1. **تحديد مسار الحركة (Directional Locking):** عند تحرك الإصبع أول 8 بكسل، يتم حساب زاوية المتجه $\theta = \arctan2(\Delta y, \Delta x)$:
   * إذا كانت الزاوية عمودية والقائمة داخل الغرفة لم تصل لحافتها القصوى (Scroll Offset > 0)، تُمنح الأولوية التامة لتمرير الغرفة (`Scrollable`).
   * إذا كانت الغرفة عند أعلى/أسفل حد لها وبدأ المستخدم بالسحب، يتم "تأميم اللمسة" فوراً وتحويلها إلى **انتقال فضائي (Spatial Pan)**.
2. **عزل إيماءة التقريب والتبعيد (Pinch-to-Floor Disambiguation):**
   * استخدام `RawGestureDetector` يعتمد على `ScaleGestureRecognizer` مخصص يرفض استهلاك الأحداث أحادية الإصبع (Single Pointer) ويُفعل خط الانتقال الرأسي (Z-Axis) حصراً عند رصد إصبعين بفارق تباعد هندسي يتجاوز حداً دقيقاً (Thresholding)، مع تتبع النقطة المحورية البؤرية (Focal Point Tracking).

---

## ⚡ المحور الثالث: ترشيد الرندر والفرز الفضائي (Spatial Culling & GPU Optimization)

### 1. المشكلة في الكود الحالي:

في `CompassTransitionLayout`، يتم وضع الغرف الخمس (المركز، الشمال، الجنوب، الشرق، الغرب) في `Stack` دائم عبر:

```dart
Stack(
  children: [
    _buildRoomLayer(centerChild, ...),
    _buildRoomLayer(northChild, ...),
    _buildRoomLayer(southChild, ...),
    _buildRoomLayer(eastChild, ...),
    _buildRoomLayer(westChild, ...),
  ],
)
```

**الأثر الكارثي على الأداء:**

* تظل خمس شاشات معقدة (تحتوي على قواعد بيانات، اشتراكات بث Streams، كاشات للصور، وتوليد نصوص) مرسومة في شجرة الـ Render Object وتستهلك ذاكرة ومجهوداً معالجاتياً في كل دورة رسم (Frame).

### 2. الحل الهندسي المعتمد:

1. **الاستبعاد المكاني (Spatial Culling / Lazy Composition):**
   * عند الوجود في المركز، يتم رسم المركز فقط مع تجهيز طفيف للغرف المحيطة في الذاكرة المنطقية دون إرسالها لمرحلة الرسم الثقيل (Paint Stage).
   * إدراج الغرف البعيدة خارج نطاق الرؤية ضمن `Offstage` أو `Visibility(maintainState: true)` لتجميد معالجة الشاشات غير المرئية حتى تصبح قريبة من مجال الرؤية.
2. **عزل حدود إعادة الرسم (Repaint Boundaries Isolation):**
   * إحاطة كل غرفة بـ `RepaintBoundary` مستقل ومفصول تماماً لمنع حدوث "تلوث طلاء" (Repaint Contamination). فإذا تحركت ساعة الإيقاف في غرفة التركيز، لا يُعاد طلاء باقي أجزاء شاشة البوصلة أو الغرف الأخرى.
3. **تحسين بطبقات العتاد (Layer Promotion):**
   * استخدام `Transform(transform: Matrix4..., filterQuality: FilterQuality.low)` أثناء الحركة الفعالة لرفع العنصر لطبقة عتادية (Composited Layer)، وإعادته إلى أعلى دقة عند التوقف التام.

---

## 📳 المحور الرابع: المزامنة اللمسية الحركية (Haptic Detents Integration)

أنظمة التشغيل المتقدمة (مثل iOS عند سحب مركز الإشعارات أو شريط التبديل) تربط الاهتزاز بنقاط فيزيائية محددة:

* **نقاط التثبيت الفضائية (Spatial Detents):**
  * أثناء سحب المستخدم للشاشة للانتقال من "المركز" إلى "الأجندة"، لا نطلق الاهتزاز عشوائياً.
  * عند وصول إزاحة الشاشة إلى **نقطة اللاعودة (Activation Threshold)** (مثلاً عند 40% من عرض الشاشة)، يطلق النظام نقرة لمسية خفيفة حادة (`HapticFeedback.selectionClick()`) ليعلم الكفيف حسيّاً دون النظر أن الإجراء سيكتمل بمجرد رفع إصبعه.
* **ارتداد الحواف (Rubber-banding Feedback):**
  * إذا سحب المستخدم في اتجاه غير موجود، تطبق الشاشة مقاومة مطاطية لوغاريتمية $d' = d \cdot (1 - \frac{1}{\frac{d \cdot c}{w} + 1})$، مع نقرة خفيفة تنبهه بالوصول للحد الأقصى.

---

# 🚀 خارطة الطريق التنفيذية (Execution Roadmap)

سننفذ هذه المعمارية الاحترافية عبر **4 محطات تنفيذية واضحة ودقيقة**:

| المحطة               | الهدف الهندسي                                                            | المخرجات البرمجية                                                                                                                                                                                |
| :------------------------- | :----------------------------------------------------------------------------------- | :--------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| **المحطة 2.1** | **تأسيس محرك النوابض (Spring Simulation Engine)**              | بناء ملف`spatial_physics.dart` لتوفير محاكيات نوابض فيزيائية لحركات (XY) و (Z) تدعم سرعات الدفع المتجه وتلغي الـ Durations الثابتة. |
| **المحطة 2.2** | **إعادة صياغة موزع الإيماءات (Gesture Arena Overhaul)** | إعادة كتابة`SpatialCompassPage` لاستخدام Recognizer موحد ومرن يمنع الصراع مع القوائم ويوفر سلاسة مطلقة لـ Pinch-to-zoom.                         |
| **المحطة 2.3** | **ترشيد الرندر والفرز الفضائي (Spatial Composition)**  | إعادة هندسة`CompassTransitionLayout` بدعم العزل المكاني (`RepaintBoundary` و `Visibility Management`) لضمان ثبات الإطارات عند 60/120fps.                    |
| **المحطة 2.4** | **ربط الـ Haptic Detents وتنعيم الـ HUD**                       | مزامنة النقرات اللمسية مع الحركة الفيزيائية، وتنعيم أنيميشن شريط البوصلة ومؤشرات الـ Waveform في الـ Zero UI.                      |

---

إذا كنت موافقاً على هذه الخطة الهندسية الدقيقة، سنبدأ فوراً بـ **المحطة 2.1: بناء محرك النوابض الفيزيائية المخصص (Spring Engine)**. هل نعطي شارة الانطلاق؟
