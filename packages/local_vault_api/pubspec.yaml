name: local_vault_api
description: "A local-first encrypted vault for BeaconOS using Drift SQLite."
version: 0.0.2
homepage:

environment:
  sdk: ^3.12.0
  flutter: ">=1.17.0"

dependencies:
  drift: ^2.35.0
  flutter:
    sdk: flutter
  path: ^1.9.1
  path_provider: ^2.1.6
  sqlite3_flutter_libs: ^0.6.0+eol
  uuid: ^4.5.1

dev_dependencies:
  build_runner: ^2.15.1
  drift_dev: ^2.35.0
  flutter_lints: ^6.0.0
  flutter_test:
    sdk: flutter

flutter: