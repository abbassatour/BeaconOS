# packages/system_hardware_api/pubspec.yaml
name: system_hardware_api
description: Hardware and OS interaction client for BeaconOS.
version: 0.0.1
publish_to: none

environment:
  sdk: ^3.12.0
  flutter: ">=1.17.0"

dependencies:
  battery_plus: ^7.1.1
  flutter:
    sdk: flutter
  permission_handler: 11.3.1
  url_launcher: ^6.3.2

dev_dependencies:
  flutter_lints: ^6.0.0
  flutter_test:
    sdk: flutter

flutter: